"""Slow adaptation of profiles to changing household behaviour.

Not in Phase 4's exit criteria (cyclic encoding, slot means, lag/rolling —
``ROADMAP.md``). Deliberately left a stub: recency weighting on
``SlotProfile`` cannot be shown to help until a demand model exists to
measure it against, which is Phase 5's job. Building it now would be
tuning against nothing.
"""
